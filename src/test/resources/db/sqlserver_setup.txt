*) Make sure the the following objects have been created in sqlserver.
use master;

create schema jdbctemplatemapper;
create LOGIN mssql WITH PASSWORD = 'somepassword'; 

CREATE USER mssql FOR LOGIN mssql;

ALTER USER mssql WITH DEFAULT_SCHEMA = jdbctemplatemapper;

GRANT SELECT, INSERT, UPDATE, DELETE, ALTER ON SCHEMA::jdbctemplatemapper TO mssql;

EXEC sp_addrolemember 'db_ddladmin', N'mssql'
